# This app's Python backend runs on Render (see render.yaml), not Vercel.
# Vercel only serves public/ as a static site — exclude the backend so
# Vercel's zero-config detection doesn't try to build it as a Python function.
api/
lib/
__pycache__/
wsgi.py
requirements.txt
render.yaml
.env
.env.example
*.pyc
