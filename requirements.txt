flask==3.0.3
flask-cors>=4.0.1
gunicorn>=22.0.0
openai>=1.40.0
yfinance>=0.2.40
pandas>=2.2.0
numpy>=1.26.0

# Answer formatting: model Markdown -> sanitized HTML for the chat UI.
markdown>=3.6
bleach>=6.1.0

# Optional: only used for local .env loading (see lib/yahoo_finance/config.py).
python-dotenv>=1.0.0